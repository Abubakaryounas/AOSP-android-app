apply plugin: 'com.android.library'

android {
    sourceSets.main {
        manifest.srcFile 'AndroidManifest.xml'
        java.srcDirs = ['src']
        res.srcDirs = ['res']
    }
    compileSdk 33
    defaultConfig {
        minSdkVersion 26
        targetSdkVersion 30
    }
}
